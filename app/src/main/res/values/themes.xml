<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CatposPoint" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="NoWindowAnimation">
        <item name="android:windowEnterAnimation">@null</item>
        <item name="android:windowExitAnimation">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="Theme.CatposPoint.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/icon_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.CatposPoint</item>
    </style>

</resources>
